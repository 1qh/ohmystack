name: UI Sync
on:
  schedule:
    - cron: '0 2 * * 2,5'
  workflow_dispatch:
concurrency:
  group: ui-sync
  cancel-in-progress: true
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
        with:
          bun-version: 'latest'
      - run: bun i
      - run: bun ui:sync
      - run: bun fix
      - run: bun test
      - uses: peter-evans/create-pull-request@v7
        with:
          branch: chore/ui-sync
          commit-message: 'chore: sync ui components from upstream'
          title: 'chore: sync ui components from upstream'
          body: |
            Automated sync from shadcn/ui and ai-elements.
            
            - Regenerated `lib/ui` with `bun ui:sync`
            - Verified with `bun fix`
            - Verified with `bun test`
          labels: dependencies
          delete-branch: true
