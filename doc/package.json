{
  "name": "@a/docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "bun scripts/gen-api-imports.ts && next build",
    "dev": "bun ../script/dev-app.ts",
    "typecheck": "next typegen && bun -e \"import { existsSync, mkdirSync, writeFileSync } from 'node:fs'; if (!existsSync('.next/types/cache-life.d.ts')) { mkdirSync('.next/types', { recursive: true }); writeFileSync('.next/types/cache-life.d.ts', 'export {}\\n'); }\" && tsc --noEmit"
  },
  "dependencies": {
    "@a/config": "workspace:*",
    "fumadocs-core": "latest",
    "fumadocs-mdx": "latest",
    "fumadocs-ui": "latest",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  }
}
