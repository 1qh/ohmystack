{
  "name": "@a/convex-movie",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nb-env next build --turbo",
    "dev": "bun ../../../script/dev-app.ts",
    "start": "nb-env next start",
    "test:e2e": "CONVEX_TEST_MODE=true nb-env --cwd=../../../backend/convex convex dev --once && nb-env playwright test --reporter=list",
    "typecheck": "nb-env next typegen && tsc --noEmit"
  },
  "devDependencies": {
    "@a/e2e": "workspace:*"
  },
  "dependencies": {
    "@a/config": "workspace:*"
  }
}
