{
  "name": "@a/convex-org",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun with-env next build --turbo",
    "dev": "bun ../../../script/dev-app.ts",
    "start": "bun with-env next start",
    "test:e2e": "CONVEX_TEST_MODE=true bun --cwd ../../../backend/convex with-env convex dev --once && bun with-env playwright test --reporter=list",
    "typecheck": "bun with-env next typegen && tsc --noEmit",
    "with-env": "dotenv -e ../../../.env --"
  },
  "dependencies": {
    "@sindresorhus/slugify": "latest"
  },
  "devDependencies": {
    "@a/e2e": "workspace:*"
  }
}
