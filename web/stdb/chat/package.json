{
  "name": "@a/stdb-chat",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nb-env next build --turbo",
    "dev": "bun ../../../script/dev-app.ts",
    "start": "nb-env next start",
    "test:e2e": "SPACETIMEDB_TEST_MODE=true bun spacetime:publish && nb-env playwright test --reporter=list",
    "typecheck": "nb-env next typegen && tsc --noEmit"
  },
  "dependencies": {
    "@a/config": "workspace:*",
    "@ai-sdk/react": "latest",
    "ai": "latest",
    "react-intersection-observer": "latest"
  },
  "devDependencies": {
    "@a/e2e": "workspace:*"
  }
}
